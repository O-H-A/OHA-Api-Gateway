spring:
  application:
    name: gateway-service

  cloud:
    gateway:
      routes:
        - id: user
          uri: lb://user-service
          predicates:
            - Path=/api/user/**
        - id: auth
          uri: lb://user-service
          predicates:
            - Path=/api/auth/**
        - id: posting
          uri: lb://posting-service
          predicates:
            - Path=/api/posting/**

eureka:
  client:
    service-url:
      defaultZone: http://52.78.12.178:8761/eureka